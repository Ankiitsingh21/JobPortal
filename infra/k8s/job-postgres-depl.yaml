apiVersion: apps/v1
kind: Deployment
metadata:
  name: job-postgres-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: job-postgres
  template:
    metadata:
      labels:
        app: job-postgres
    spec:
      containers:
        - name: job-postgres
          image: postgres
          env:
            - name: POSTGRES_DB
              value: job
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-job-srv
spec:
  selector:
    app: job-postgres
  ports:
    - name: db
      protocol: TCP
      port: 5432
      targetPort: 5432