apiVersion: apps/v1
kind: Deployment
metadata:
  name: application-postgres-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: application-postgres
  template:
    metadata:
      labels:
        app: application-postgres
    spec:
      containers:
        - name: application-postgres
          image: postgres
          env:
            - name: POSTGRES_DB
              value: application
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-application-srv
spec:
  selector:
    app: application-postgres
  ports:
    - name: db
      protocol: TCP
      port: 5432
      targetPort: 5432